import { Typography } from "@mui/material"

export function DownloadStep() {
    return (
        <>
            <Typography variant="h1">Your cipher is ready!</Typography>
            <Typography variant="h2">if you wanna decrypt it, use the "Decrypt" tab!</Typography>
        </>
    )
}